[platform]
name = "gacha-agency"
base_dir = "/data/tenants"
template_dir = "/data/templates"
max_global_workers = 50
max_global_cost_per_day_usd = 100.0
jwt_secret = "CHANGE_ME_TO_A_RANDOM_SECRET"

[providers.anthropic]
api_key = "sk-ant-CHANGE_ME"

[providers.openrouter]
api_key = "sk-or-CHANGE_ME"

[web]
bind = "0.0.0.0:8420"
cors_origins = ["https://gacha.agency", "http://localhost:3100"]

[tiers.free]
max_companions = 5
max_projects = 1
max_workers = 1
max_cost_per_day_usd = 1.0
max_storage_mb = 500
can_execute_code = false
model = "claude-haiku-4-5"

[tiers.basic]
max_companions = 20
max_projects = 3
max_workers = 2
max_cost_per_day_usd = 5.0
max_storage_mb = 5000
can_execute_code = true
model = "claude-sonnet-4-6"

[tiers.pro]
max_companions = 100
max_projects = 10
max_workers = 5
max_cost_per_day_usd = 20.0
max_storage_mb = 50000
can_execute_code = true
model = "claude-opus-4-6"
